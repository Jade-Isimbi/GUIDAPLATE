# Trained Model Artifacts

Place trained model files here after running the training notebooks.

Expected files (generated by notebooks 04 and 05):
- `xgboost.pkl` — XGBoost dietary risk classifier
- `lstm_model.h5` — LSTM pattern detection model
- `scaler.pkl` — Feature scaler for inference

These files are excluded from Git via `.gitignore` (large binaries).
